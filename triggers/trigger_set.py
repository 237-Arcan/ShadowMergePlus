class TriggerSet:
    def detect_anomalies(self, odds_data: dict) -> dict:
        """
        Détecte les signaux suspects selon des patterns (faux favori, renversement, etc.)
        :param odds_data: dict des cotes par source
        :return: dict de signaux détectés
        """
        # Squelette à compléter selon ta logique métier
        signals = {}
        # ... détection
        return signals
