class ContextInsightManager:
    def analyze(self, whoscored_data, soccerdata_data) -> dict:
        """
        Analyse contextuelle à partir des ratings, stats, absences, etc.
        :return: dict d’insights qualitatifs
        """
        insights = {}
        # ... analyse qualitative à compléter
        return insights
